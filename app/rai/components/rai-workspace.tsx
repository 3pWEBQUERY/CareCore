"use client";

import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import ModulePageShell, { ModuleIcon } from "@/app/components/module-page-shell";
import { CareDatePicker, CareSelect, formatCareDate } from "@/app/components/care-form-controls";

export type RaiView = "overview" | "assessment" | "due" | "reports";
type RaiResident = { id: string; initials: string; name: string; room: string; unit: string; status: string; tone: "stable" | "attention" | "critical" | "info"; progress: number; next: string; assessor: string };

const residents: RaiResident[] = [
  { id: "hm", initials: "HM", name: "Hans Müller", room: "Zimmer 207", unit: "Wohnbereich 2", status: "In Bearbeitung", tone: "attention", progress: 68, next: "Heute · 14:00", assessor: "Anna Meier" },
  { id: "mk", initials: "MK", name: "Maria Keller", room: "Zimmer 204", unit: "Wohnbereich 2", status: "Aktuell", tone: "stable", progress: 100, next: "28.09.2026", assessor: "Anna Meier" },
  { id: "em", initials: "EM", name: "Erika Meier", room: "Zimmer 211", unit: "Wohnbereich 2", status: "Fällig", tone: "critical", progress: 42, next: "Heute · 16:00", assessor: "Nora Baumann" },
  { id: "rb", initials: "RB", name: "Ruth Baumann", room: "Zimmer 214", unit: "Wohnbereich 2", status: "Aktuell", tone: "stable", progress: 100, next: "07.10.2026", assessor: "Lea Frei" },
  { id: "pa", initials: "PA", name: "Peter Aebischer", room: "Zimmer 115", unit: "Wohnbereich 1", status: "Fällig", tone: "attention", progress: 31, next: "Morgen · 09:30", assessor: "Lea Frei" },
  { id: "wb", initials: "WB", name: "Walter Brunner", room: "Zimmer 306", unit: "Wohnbereich 3", status: "Neuaufnahme", tone: "info", progress: 12, next: "Nach Eintritt", assessor: "Nora Baumann" },
];

const instruments = ["interRAI LTCF", "interRAI HC", "interRAI CMH", "interRAI Screener"];
const assessorOptions = ["Anna Meier", "Lea Frei", "Nora Baumann", "Sven Keller"];

function OverviewView({ showToast }: { showToast: (message: string) => void }) {
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("Alle");
  const filtered = useMemo(() => residents.filter((resident) => (filter === "Alle" || resident.status === filter) && `${resident.name} ${resident.room} ${resident.unit}`.toLocaleLowerCase("de-CH").includes(query.trim().toLocaleLowerCase("de-CH"))), [filter, query]);
  return <><section className="rai-summary" aria-label="RAI Übersicht"><div><span className="rai-summary-icon"><ModuleIcon name="assess"/></span><span><strong>48</strong><small>aktive RAI-Akten</small></span></div><div><span className="rai-summary-icon attention"><ModuleIcon name="calendar"/></span><span><strong>7</strong><small>Erfassungen fällig</small></span></div><div><span className="rai-summary-icon info"><ModuleIcon name="check"/></span><span><strong>86%</strong><small>hausweit aktuell</small></span></div><div><span className="rai-summary-icon stable"><ModuleIcon name="team"/></span><span><strong>4</strong><small>RAI Verantwortliche</small></span></div></section><div className="rai-overview-layout"><section className="card rai-worklist"><div className="rai-card-header"><div><p className="eyebrow">Arbeitskorb</p><h2 className="card-title">Bewohner und Erfassungen</h2><p className="card-subtitle">{filtered.length} von {residents.length} Einträgen sichtbar</p></div><label className="resident-search"><ModuleIcon name="search"/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Bewohner suchen" aria-label="RAI Bewohner suchen"/></label></div><div className="rai-filter-row">{["Alle", "Fällig", "In Bearbeitung", "Aktuell"].map((item) => <button className={filter === item ? "active" : ""} type="button" key={item} aria-pressed={filter === item} onClick={() => setFilter(item)}>{item}</button>)}</div><div className="rai-resident-list">{filtered.map((resident) => <button type="button" className="rai-resident-row" key={resident.id} onClick={() => showToast(`${resident.name} für interRAI ausgewählt`)}><span className="resident-avatar">{resident.initials}</span><span><strong>{resident.name}</strong><small>{resident.room} · {resident.unit}</small><em>{resident.assessor} · nächste Erfassung {resident.next}</em></span><span className="rai-progress"><i><span style={{ width: `${resident.progress}%` }}/></i><b>{resident.progress}%</b></span><span className={`status-badge ${resident.tone}`}>{resident.status}</span><ModuleIcon name="chevron"/></button>)}{filtered.length === 0 && <div className="resident-empty"><ModuleIcon name="search"/><strong>Keine RAI-Akten gefunden</strong><p>Suchbegriff oder Filter anpassen.</p></div>}</div></section><aside className="rai-side-stack"><section className="card rai-responsible-card"><div className="card-header"><div><p className="eyebrow">Rollen und Rechte</p><h2 className="card-title">RAI Verantwortliche</h2></div><span className="status-badge stable">4 aktiv</span></div><div className="rai-responsible-list">{assessorOptions.slice(0, 4).map((name, index) => <div key={name}><span className="avatar">{name.split(" ").map((part) => part[0]).join("")}</span><span><strong>{name}</strong><small>{index === 0 ? "Leitung RAI · LTCF" : index === 1 ? "RAI Verantwortliche · HC" : "RAI Verantwortliche · LTCF"}</small></span><span className="rai-online-dot"/></div>)}</div><button className="secondary-button" type="button" onClick={() => showToast("RAI-Berechtigungen geöffnet")}>Berechtigungen verwalten <ModuleIcon name="chevron"/></button></section><section className="card rai-next-card"><div className="card-header"><div><p className="eyebrow">Nächster Schritt</p><h2 className="card-title">Erfassung fortsetzen</h2></div></div><span className="rai-next-icon"><ModuleIcon name="assess"/></span><strong>Hans Müller · interRAI LTCF</strong><p>Die Erfassung ist zu 68% abgeschlossen. Die letzten Bereiche warten auf deine fachliche Einschätzung.</p><button className="primary-button" type="button" onClick={() => showToast("interRAI-Erfassung für Hans Müller geöffnet")}>Weiterarbeiten <ModuleIcon name="chevron"/></button></section></aside></div></>;
}

function AssessmentView({ showToast }: { showToast: (message: string) => void }) {
  const [resident, setResident] = useState("Hans Müller · Zimmer 207");
  const [instrument, setInstrument] = useState("interRAI LTCF");
  const [assessor, setAssessor] = useState("Anna Meier");
  const [date, setDate] = useState("2026-09-15");
  const [scores, setScores] = useState<Record<string, string>>({ Alltag: "2 – Geringe Unterstützung", Kognition: "1 – Beobachten", Stimmung: "0 – Kein Bedarf", Gesundheit: "2 – Geringe Unterstützung" });
  const domains = [{ id: "Alltag", description: "Essen, Körperpflege, Ankleiden und Mobilität" }, { id: "Kognition", description: "Orientierung, Gedächtnis und Entscheidungsfähigkeit" }, { id: "Stimmung", description: "Antrieb, Rückzug und psychosoziales Wohlbefinden" }, { id: "Gesundheit", description: "Schmerz, Haut, Sturzrisiko und klinische Hinweise" }];
  return <section className="rai-assessment-layout"><section className="card rai-assessment-form"><div className="rai-card-header"><div><p className="eyebrow">Neue Erfassung</p><h2 className="card-title">interRAI strukturiert dokumentieren</h2><p className="card-subtitle">Erfasse die Beobachtungen direkt im Kontext der Bewohnerakte.</p></div><span className="status-badge info">Entwurf</span></div><div className="area-editor-grid rai-form-grid"><label>Bewohner<CareSelect label="Bewohner" value={resident} options={residents.map((person) => `${person.name} · ${person.room}`)} onChange={setResident}/></label><label>Instrument<CareSelect label="Instrument" value={instrument} options={instruments} onChange={setInstrument}/></label><label>RAI Verantwortliche<CareSelect label="RAI Verantwortliche" value={assessor} options={assessorOptions} onChange={setAssessor}/></label><label>Erfassungsdatum<CareDatePicker label="Erfassungsdatum" value={date} onChange={setDate}/></label></div><div className="rai-domain-grid">{domains.map((domain) => <article className="rai-domain-card" key={domain.id}><div><span className="rai-domain-icon"><ModuleIcon name={domain.id === "Kognition" ? "assess" : domain.id === "Gesundheit" ? "vitals" : domain.id === "Stimmung" ? "pulse" : "tasks"}/></span><span><strong>{domain.id}</strong><small>{domain.description}</small></span></div><CareSelect label={`${domain.id} Einschätzung`} value={scores[domain.id]} options={["0 – Kein Bedarf", "1 – Beobachten", "2 – Geringe Unterstützung", "3 – Hoher Unterstützungsbedarf", "4 – Umfassende Unterstützung"]} onChange={(value) => setScores((current) => ({ ...current, [domain.id]: value }))}/></article>)}</div><label className="rai-notes-field">Fachliche Notiz<textarea placeholder="Beobachtungen, Ressourcen und Begründung der Einschätzung …" rows={6}/></label><div className="rai-form-footer"><span><ModuleIcon name="check"/> Autospeicherung aktiv · letzter Entwurf vor 2 Min.</span><button className="primary-button" type="button" onClick={() => showToast(`interRAI-Erfassung für ${resident.split(" · ")[0]} gespeichert`)}>Erfassung speichern <ModuleIcon name="check"/></button></div></section><aside className="rai-assessment-side"><section className="card"><div className="card-header"><div><p className="eyebrow">Kontext</p><h2 className="card-title">Erfassungsstatus</h2></div></div><div className="rai-context-list"><div><span>Bewohner</span><strong>{resident.split(" · ")[0]}</strong></div><div><span>Instrument</span><strong>{instrument}</strong></div><div><span>Datum</span><strong>{formatCareDate(date)}</strong></div><div><span>Verantwortlich</span><strong>{assessor}</strong></div></div></section><section className="card rai-safety-card"><ModuleIcon name="quality"/><h2 className="card-title">Fachliche Verantwortung</h2><p>Die Einschätzung bleibt ein fachlicher Entwurf, bis sie durch eine berechtigte RAI Verantwortliche freigegeben wird.</p></section></aside></section>;
}

function DueView({ showToast }: { showToast: (message: string) => void }) {
  const [filter, setFilter] = useState("Alle");
  const items = [{ ...residents[2], due: "Heute · 16:00", reason: "Jährliche Folgeerfassung" }, { ...residents[4], due: "Morgen · 09:30", reason: "Nach Eintritt ergänzen" }, { ...residents[0], due: "18.09.2026", reason: "Offene Bereiche abschliessen" }];
  const visible = items.filter((item) => filter === "Alle" || (filter === "Heute" ? item.due.startsWith("Heute") : filter === "Diese Woche" ? !item.due.startsWith("Heute") : item.reason.includes("Eintritt")));
  return <section className="card rai-due-card"><div className="rai-card-header"><div><p className="eyebrow">Arbeitskorb</p><h2 className="card-title">RAI-Fälligkeiten</h2><p className="card-subtitle">{visible.length} von {items.length} Erfassungen benötigen Aufmerksamkeit</p></div><div className="rai-filter-row">{["Alle", "Heute", "Diese Woche", "Nach Eintritt"].map((item) => <button className={filter === item ? "active" : ""} type="button" key={item} onClick={() => setFilter(item)}>{item}</button>)}</div></div><div className="rai-due-table-head"><span>Bewohner</span><span>Grund</span><span>Fällig</span><span>Zuständig</span><span/></div><div className="rai-due-list">{visible.map((item) => <article key={item.id}><span className="resident-person"><span className="resident-avatar">{item.initials}</span><span><strong>{item.name}</strong><small>{item.room} · {item.unit}</small></span></span><span>{item.reason}</span><span className={`status-badge ${item.tone}`}>{item.due}</span><span>{item.assessor}</span><button className="quiet-button" type="button" onClick={() => showToast(`Fälligkeit für ${item.name} geöffnet`)}>Öffnen <ModuleIcon name="chevron"/></button></article>)}{visible.length === 0 && <div className="resident-empty"><ModuleIcon name="calendar"/><strong>Keine Fälligkeiten</strong><p>Der Filter zeigt aktuell keine Einträge.</p></div>}</div></section>;
}

function ReportsView({ showToast }: { showToast: (message: string) => void }) {
  const reports = [{ title: "RAI-Vollständigkeit Haus", detail: "Aktive Akten und offene Bereiche nach Wohnbereich", value: "86%", tone: "stable" }, { title: "Unterstützungsbedarf", detail: "Verteilung der interRAI-Scores im aktuellen Quartal", value: "48", tone: "info" }, { title: "Fälligkeiten September", detail: "Erfassungen mit Termin und verantwortlicher Person", value: "7", tone: "attention" }];
  return <div className="rai-reports-layout"><section className="card rai-report-grid"><div className="rai-card-header"><div><p className="eyebrow">Auswertung</p><h2 className="card-title">RAI-Berichte</h2><p className="card-subtitle">Transparente Kennzahlen für Pflege und Leitung.</p></div><button className="secondary-button" type="button" onClick={() => showToast("RAI-Bericht wird exportiert")}>Exportieren <ModuleIcon name="docs"/></button></div><div className="rai-report-cards">{reports.map((report) => <button type="button" className="rai-report-card" key={report.title} onClick={() => showToast(`${report.title} geöffnet`)}><span className={`rai-report-value ${report.tone}`}>{report.value}</span><strong>{report.title}</strong><small>{report.detail}</small><ModuleIcon name="chevron"/></button>)}</div></section><aside className="card rai-report-note"><div className="card-header"><div><p className="eyebrow">Datenqualität</p><h2 className="card-title">Freigaben</h2></div></div><div className="rai-report-meter"><span style={{ width: "86%" }}/></div><strong>86% vollständig freigegeben</strong><p>4 Entwürfe warten auf die Prüfung durch eine RAI Verantwortliche.</p><button className="primary-button" type="button" onClick={() => showToast("Freigabewarteschlange geöffnet")}>Freigaben prüfen</button></aside></div>;
}

function RaiRefreshPopover({ open, onClose, showToast }: { open: boolean; onClose: () => void; showToast: (message: string) => void }) {
  const [scope, setScope] = useState("Alle offenen Erfassungen");
  const [period, setPeriod] = useState("Aktueller Monat");
  const [units, setUnits] = useState(["Wohnbereich 1", "Wohnbereich 2", "Wohnbereich 3", "Pflegewohngruppe"]);
  const [notify, setNotify] = useState(true);
  const [logChanges, setLogChanges] = useState(true);

  useEffect(() => {
    if (!open) return;
    const onKey = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [open, onClose]);

  if (!open) return null;
  const toggleUnit = (unit: string) => setUnits((current) => current.includes(unit) ? current.filter((item) => item !== unit) : [...current, unit]);
  return <div className="area-editor-overlay rai-refresh-overlay" role="presentation" onMouseDown={(event) => event.currentTarget === event.target && onClose()}><section className="area-editor-panel rai-refresh-panel" role="dialog" aria-modal="true" aria-labelledby="rai-refresh-title"><header className="area-editor-header"><div><p className="eyebrow">CareCore RAI · Arbeitskorb</p><h2 id="rai-refresh-title">Fälligkeiten aktualisieren</h2><p>Synchronisiere die RAI-Fälligkeiten mit dem aktuellen Bewohnerbestand und informiere die zuständigen RAI Verantwortlichen.</p></div><button className="area-editor-close" type="button" onClick={onClose} aria-label="Aktualisierung schliessen">×</button></header><form className="area-editor-form" onSubmit={(event) => { event.preventDefault(); onClose(); showToast("RAI-Fälligkeiten wurden aktualisiert"); }}><div className="area-editor-intro"><span className="area-editor-icon"><ModuleIcon name="calendar"/></span><div><strong>Neue Aktualisierung</strong><p>Die Prüfung berücksichtigt Eintritte, Austritte und offene Erfassungsbereiche.</p></div><span className="duty-assignment-status"><i/>Bereit zur Synchronisierung</span></div><div className="area-editor-grid rai-refresh-grid"><label>Aktualisierungsumfang<CareSelect label="Aktualisierungsumfang" value={scope} options={["Alle offenen Erfassungen", "Nur überfällige Erfassungen", "Nur neue Bewohner"]} onChange={setScope}/></label><label>Zeitraum<CareSelect label="Zeitraum" value={period} options={["Aktueller Monat", "Nächste 30 Tage", "Aktuelles Quartal"]} onChange={setPeriod}/></label><fieldset className="area-editor-wide rai-refresh-units"><legend>Wohnbereiche einbeziehen</legend><div className="area-service-options">{["Wohnbereich 1", "Wohnbereich 2", "Wohnbereich 3", "Pflegewohngruppe"].map((unit) => <label className={units.includes(unit) ? "selected" : ""} key={unit}><input type="checkbox" checked={units.includes(unit)} onChange={() => toggleUnit(unit)}/><span>{unit}</span></label>)}</div></fieldset><fieldset className="area-editor-wide rai-refresh-options"><legend>Zusätzliche Optionen</legend><div className="area-service-options"><label className={notify ? "selected" : ""}><input type="checkbox" checked={notify} onChange={() => setNotify((value) => !value)}/><span>Verantwortliche benachrichtigen</span></label><label className={logChanges ? "selected" : ""}><input type="checkbox" checked={logChanges} onChange={() => setLogChanges((value) => !value)}/><span>Änderungen protokollieren</span></label><label><input type="checkbox" defaultChecked/><span>Fälligkeiten neu priorisieren</span></label></div></fieldset><section className="rai-refresh-preview area-editor-wide" aria-label="Vorschau der Aktualisierung"><div><span className="rai-refresh-preview-icon"><ModuleIcon name="check"/></span><span><strong>Vorschau</strong><small>{scope} · {period}</small></span></div><span><strong>{units.length}</strong><small>Wohnbereiche</small></span><span><strong>7</strong><small>mögliche Fälligkeiten</small></span></section></div><footer className="area-editor-actions"><button className="secondary-button" type="button" onClick={onClose}>Abbrechen</button><button className="primary-button" type="submit" disabled={units.length === 0}><ModuleIcon name="check"/> Fälligkeiten aktualisieren</button></footer></form></section></div>;
}

const meta: Record<RaiView, { child: string; title: string; description: string; action: string }> = { overview: { child: "Übersicht", title: "RAI Arbeitsplatz", description: "interRAI-Erfassungen, Verantwortlichkeiten und Fälligkeiten zentral steuern.", action: "Neue interRAI-Erfassung" }, assessment: { child: "interRAI-Erfassung", title: "interRAI-Erfassung", description: "Bewohnerbezogene Einschätzungen strukturiert und nachvollziehbar dokumentieren.", action: "Erfassung speichern" }, due: { child: "Fälligkeiten", title: "RAI-Fälligkeiten", description: "Anstehende Erfassungen und offene Bereiche zuverlässig im Blick behalten.", action: "Fälligkeiten aktualisieren" }, reports: { child: "Berichte", title: "RAI-Berichte", description: "Auswertungen zur Datenqualität und zum Unterstützungsbedarf im Haus.", action: "Bericht exportieren" } };

export default function RaiWorkspace({ view }: { view: RaiView }) {
  const router = useRouter();
  const current = meta[view];
  const [refreshOpen, setRefreshOpen] = useState(false);
  return <ModulePageShell activeModule="rai" activeChild={current.child} pageClass={`rai-page rai-${view}`} locationSecondary="Gesamtes Haus · alle Wohnbereiche">
    {(showToast) => <><main className="workspace module-workspace rai-workspace"><section className="page-heading care-page-heading" aria-labelledby="rai-title"><div className="heading-copy"><p className="eyebrow">CareCore RAI</p><h1 id="rai-title">{current.title}</h1><p>{current.description}</p></div><button className="primary-button" type="button" onClick={() => view === "overview" ? router.push("/rai/erfassung") : view === "due" ? setRefreshOpen(true) : showToast(`${current.action} vorbereitet`)}><ModuleIcon name={view === "overview" ? "plus" : "check"} className="button-icon"/>{current.action}</button></section>{view === "overview" && <OverviewView showToast={showToast}/>} {view === "assessment" && <AssessmentView showToast={showToast}/>} {view === "due" && <DueView showToast={showToast}/>} {view === "reports" && <ReportsView showToast={showToast}/>}</main><RaiRefreshPopover open={refreshOpen} onClose={() => setRefreshOpen(false)} showToast={showToast}/></>}
  </ModulePageShell>;
}
