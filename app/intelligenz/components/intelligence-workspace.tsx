"use client";

import { useState } from "react";
import ModulePageShell, { ModuleIcon } from "@/app/components/module-page-shell";

type IntelligenceView = "assistant" | "drafts";

const viewMeta: Record<IntelligenceView, { child: string; title: string; description: string }> = {
  assistant: { child: "Assistenz", title: "CareCore Assistenz", description: "Kontextbezogene Unterstützung für deine nächste Pflegeentscheidung." },
  drafts: { child: "KI-Entwürfe", title: "KI-Entwürfe", description: "Vorschläge prüfen, anpassen und sicher in deine Dokumentation übernehmen." },
};

export default function IntelligenceWorkspace({ view }: { view: IntelligenceView }) {
  const meta = viewMeta[view];
  const [prompt, setPrompt] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [selected, setSelected] = useState("Übergabe zusammenfassen");
  const prompts = ["Übergabe zusammenfassen", "Risiken für die Visite prüfen", "Dokumentation vorbereiten"];
  return <ModulePageShell activeModule="ai" activeChild={meta.child} pageClass={`intelligence-page intelligence-${view}`} locationSecondary="Gesamtes Haus · alle Wohnbereiche">
    {(showToast) => <main className="workspace intelligence-workspace">
      <section className="intelligence-heading page-heading"><div className="heading-copy"><p className="eyebrow">CareCore KI</p><h1>{meta.title}</h1><p>{meta.description}</p></div><span className="intelligence-trust"><ModuleIcon name="check"/> Datenschutz geprüft</span></section>
      {view === "assistant" ? <div className="intelligence-layout"><section className="card intelligence-chat"><div className="card-header"><div><p className="eyebrow">Assistenz</p><h2 className="card-title">Womit kann ich helfen?</h2><p className="card-subtitle">Die Antwort bleibt im Kontext deiner aktuellen Arbeitsfläche.</p></div><span className="intelligence-status"><i/>Bereit</span></div><div className="intelligence-suggestions">{prompts.map((item) => <button className={selected === item ? "active" : ""} type="button" key={item} onClick={() => setSelected(item)}><ModuleIcon name={item.includes("Risiken") ? "alert" : item.includes("Dokumentation") ? "note" : "handover"}/>{item}</button>)}</div><div className="intelligence-conversation"><div className="intelligence-message assistant"><span className="intelligence-avatar"><ModuleIcon name="ai"/></span><p>Hallo Anna! Ich kann Übergaben strukturieren, Risiken hervorheben und Dokumentationsentwürfe vorbereiten. Die finale Freigabe bleibt immer bei dir.</p></div>{submitted && <div className="intelligence-message user"><p>{prompt}</p></div>}</div><form className="intelligence-composer" onSubmit={(event) => { event.preventDefault(); if (!prompt.trim()) return; setSubmitted(true); showToast("Assistenzantwort vorbereitet"); }}><input value={prompt} onChange={(event) => setPrompt(event.target.value)} placeholder="Frage oder Auftrag eingeben…" aria-label="Auftrag an CareCore KI"/><button className="primary-button" type="submit" disabled={!prompt.trim()}><ModuleIcon name="ai"/>Senden</button></form></section><aside className="intelligence-side"><section className="card"><div className="card-header"><div><p className="eyebrow">Kontext</p><h2 className="card-title">Aktuelle Auswahl</h2></div></div><div className="intelligence-context"><strong>Wohnbereich 2</strong><span>12 Bewohner · Frühdienst</span><span>5 offene Aufgaben</span><span>2 Hinweise zur Prüfung</span></div></section><section className="card"><div className="card-header"><div><p className="eyebrow">Hinweis</p><h2 className="card-title">Sicher arbeiten</h2></div></div><p className="intelligence-note">KI-Vorschläge sind Entwürfe und werden erst nach deiner Prüfung in CareCore gespeichert.</p></section></aside></div> : <div className="intelligence-layout"><section className="card intelligence-drafts"><div className="operations-toolbar"><div><p className="eyebrow">Prüfwarteschlange</p><h2 className="card-title">Entwürfe zur Freigabe</h2><p className="card-subtitle">3 Vorschläge warten auf deine Entscheidung</p></div><button className="secondary-button" type="button" onClick={() => showToast("Entwürfe aktualisiert")}>Aktualisieren</button></div><div className="intelligence-draft-list">{[["Übergabe · Hans Müller", "Sturzrisiko und Kontrollbedarf strukturiert", "Heute, 08:10", "Prüfen"], ["Verlaufsnotiz · Maria Keller", "Blutzuckerverlauf mit Visithinweis", "Gestern, 16:20", "Prüfen"], ["Pflegeplanung · Erika Meier", "Ziel zur Mobilität vorgeschlagen", "11.09.2026", "Freigabe"]].map(([title, detail, time, action]) => <article key={title}><span className="governance-icon"><ModuleIcon name="sparkle"/></span><span><strong>{title}</strong><small>{detail}</small><em>{time} · CareCore KI</em></span><button className="quiet-button" type="button" onClick={() => showToast(`${title} geöffnet`)}>{action}</button></article>)}</div></section><aside className="intelligence-side"><section className="card intelligence-review"><div className="card-header"><div><p className="eyebrow">Freigabe</p><h2 className="card-title">Prüfregeln</h2></div></div><ul><li><ModuleIcon name="check"/>Keine automatische Veröffentlichung</li><li><ModuleIcon name="check"/>Änderungen bleiben nachvollziehbar</li><li><ModuleIcon name="check"/>Fachliche Verantwortung bleibt beim Team</li></ul></section></aside></div>}
    </main>}
  </ModulePageShell>;
}
